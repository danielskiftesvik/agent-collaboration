Implement `calculateLoyaltyPoints` in src/loyalty-points.mjs to satisfy its doc comment and the test suite in test/loyalty-points.test.mjs. Currently it throws "not implemented". Read every numbered rule in the doc comment carefully and follow the exact order/interaction it describes — some plausible-seeming implementations get the ordering of the rules wrong. Do not change the test file.

Then run: node --test test/loyalty-points.test.mjs
Expected: all 8 tests pass.

When finished, return ONLY a JSON object and NOTHING else — no prose before or after it, no markdown code fence. Match this exact shape:
{"status":"completed" | "failed" | "blocked","summary":"<one line>","changed":true | false}
