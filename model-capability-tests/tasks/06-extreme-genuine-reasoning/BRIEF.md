Implement `maxPriorityValue` in src/meeting-scheduler.mjs to satisfy its doc comment and the test suite in test/meeting-scheduler.test.mjs. Currently it throws "not implemented". Read the doc comment carefully — it warns that some intuitive-seeming approaches are wrong for this problem. Do not change the test file.

Then run: node --test test/meeting-scheduler.test.mjs
Expected: all 7 tests pass.

When finished, return ONLY a JSON object and NOTHING else — no prose before or after it, no markdown code fence. Match this exact shape:
{"status":"completed" | "failed" | "blocked","summary":"<one line>","changed":true | false}
