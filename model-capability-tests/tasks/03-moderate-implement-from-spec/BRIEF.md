Implement `parseQueryString` in src/query-string.mjs to satisfy its doc comment and the test suite in test/query-string.test.mjs. Currently it throws "not implemented". Do not change the test file.

Then run: node --test test/query-string.test.mjs
Expected: all 5 tests pass.

When finished, return ONLY a JSON object and NOTHING else — no prose before or after it, no markdown code fence. Match this exact shape:
{"status":"completed" | "failed" | "blocked","summary":"<one line>","changed":true | false}
